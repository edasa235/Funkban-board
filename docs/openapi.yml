openapi: 3.1.0
info:
  title: funkban
  version: 1.0.0

servers:
  - url: http://localhost:3000
paths:
  /api/tasks:
    get:
      summary: Get all tasks
      responses:
        "200":
          description: List of tasks
